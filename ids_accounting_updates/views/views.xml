<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="ids_accounting_updates.list">
      <field name="name">ids_accounting_updates list</field>
      <field name="model">ids_accounting_updates.ids_accounting_updates</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="ids_accounting_updates.action_window">
      <field name="name">ids_accounting_updates window</field>
      <field name="res_model">ids_accounting_updates.ids_accounting_updates</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="ids_accounting_updates.action_server">
      <field name="name">ids_accounting_updates server</field>
      <field name="model_id" ref="model_pl_accounting_updates_pl_accounting_updates"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="ids_accounting_updates" id="ids_accounting_updates.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="ids_accounting_updates.menu_1" parent="ids_accounting_updates.menu_root"/>
    <menuitem name="Menu 2" id="ids_accounting_updates.menu_2" parent="ids_accounting_updates.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="ids_accounting_updates.menu_1_list" parent="ids_accounting_updates.menu_1"
              action="ids_accounting_updates.action_window"/>
    <menuitem name="Server to list" id="ids_accounting_updates" parent="ids_accounting_updates.menu_2"
              action="ids_accounting_updates.action_server"/>
-->
  </data>
</odoo>